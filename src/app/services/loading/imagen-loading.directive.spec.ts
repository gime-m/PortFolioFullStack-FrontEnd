import { ImagenLoadingDirective } from './imagen-loading.directive';

describe('ImagenLoadingDirective', () => {
  it('should create an instance', () => {
    const directive = new ImagenLoadingDirective();
    expect(directive).toBeTruthy();
  });
});
